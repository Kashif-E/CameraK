package com.kashif.cameraK.enums

/**
 * Enum representing the rotation of the camera preview and image capture.
 */
enum class Rotation(val value: Int) {
    ROTATION_0(0),
    ROTATION_90(90),
    ROTATION_180(180),
    ROTATION_270(270)
}